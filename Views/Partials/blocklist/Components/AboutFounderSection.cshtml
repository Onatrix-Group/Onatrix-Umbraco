@inherits Umbraco.Cms.Web.Common.Views.UmbracoViewPage<Umbraco.Cms.Core.Models.Blocks.BlockListItem>
@using ContentModels = Umbraco.Cms.Web.Common.PublishedModels;

@{
    var content = (ContentModels.AboutFounderSection)Model.Content;
}

<section class="founder-section py-24">
    <div class="container mx-auto max-w-6xl flex flex-row md:flex-row items-center">
        <div class="flex gap-10">
            <div class="w-[30%]">
                @if (content.AboutFounderImage != null)
                {
                    <img src="@content.AboutFounderImage.Url()" alt="Founder standing"/>
                }
                <span class="block mt-4">@content.AboutFounderNameAndRole</span>
            </div>

            <div class="description w-[70%]">@content.AboutFounderDescription</div>
        </div>
    </div>
</section>